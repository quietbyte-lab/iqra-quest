# ── The book itself ────────────────────────────────────────────────────────
# Only the TRANSCRIBED WORDS are published (app/data/book.js and
# pipeline/transcripts/). The scanned book, and any page images rendered from it,
# never go into the repository.
*.pdf
*.png
*.jpg
*.jpeg
*.tif
*.tiff
pages/
scans/

# ── The teacher's voice and video ──────────────────────────────────────────
# Kept local until the channel owner approves public distribution.
pipeline/media/
pipeline/media_fast/
pipeline/clips/
pipeline/clips_fast/
pipeline/work/
pipeline/review/
pipeline/joined/
app/audio/
*.mp3
*.m4a
*.wav

# ── Local only ─────────────────────────────────────────────────────────────
__pycache__/
.claude/settings.local.json
